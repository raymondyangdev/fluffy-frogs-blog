<div class="p-4 md:p-8">
  <h1 class="text-2xl font-semibold mb-4"><label for="comments">Update your comment!</label></h1>
  <form action="/updateComment" method="POST" class="space-y-4">
    <input type="hidden" name="articleId" id="articleIdComments" value="{{article.id}}">
    <input type="hidden" name="commentId" id="commentId" value="{{comment.id}}">
    <textarea id="comments" placeholder="Tell us your thoughts!" name="comments" rows="5" cols="50" required
      class="w-full border rounded-md px-3 py-2 focus:outline-none focus:ring focus:ring-indigo-300">{{comment.content}}</textarea>
    <button type="submit"
      class="bg-indigo-500 text-white px-4 py-2 rounded-md hover:bg-indigo-600 focus:outline-none focus:ring focus:ring-indigo-300">Update</button>
  </form>
</div>